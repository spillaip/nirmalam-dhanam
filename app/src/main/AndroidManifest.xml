<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Used only after the user enables Nirmalam AI and presses a preset BYOL insight. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@drawable/nirmalam_launcher_icon_master"
        android:roundIcon="@drawable/nirmalam_launcher_icon_master"
        android:label="Nirmalam Dhanam"
        android:theme="@style/Theme.NirmalamDhanam">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
